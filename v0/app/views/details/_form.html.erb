<%= form_for([:apartment, @detail]) do |f| %>
  <% if @detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@detail.errors.count, "error") %> prohibited this detail from being saved:</h2>

      <ul>
      <% @detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :procon %><br>
    <%= f.text_field :procon %>
  </div>
    <%= f.hidden_field :apartment_id, :value => @apartment.id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
