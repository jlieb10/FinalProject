<%= form_for([:hunt, :apartment, @detail]) do |f| %>
  <% if @detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@detail.errors.count, "error") %> prohibited this detail from being saved:</h2>

      <ul>
      <% @detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :procon %><br>
    <%= f.text_field :procon %>
  </div>
  <div class="field">
    <%= f.label :apartment_id %><br>
    <%= f.text_field :apartment_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
